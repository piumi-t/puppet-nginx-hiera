server {
  listen <%= @port %>;
      root  <%= @vhost_docroot %>;
            server_name <%= @name %> <%= @serveraliases %>;

	            access_log  <%= @config_log_dir %>/<%= @name %>.access.log;
		    	  error_log  <%= @config_log_dir %>/<%= @name %>.error.log;

			  	  }
